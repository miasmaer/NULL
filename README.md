# NULL
NULL is a lightweight terminal network sniffer for Linux, built on libpcap and ncurses. It captures live traffic, decodes Ethernet/IP/TCP/UDP/ICMP/ARP and more, sniffs DNS, HTTP, TLS SNI, and QUIC hostnames, and displays it in live dashboard or plain text with optional CSV logging. 
